{
  "name": "@sih2025/db",
  "version": "0.1.0",
  "description": "Database schema and migrations for Smart Tourist Safety Platform",
  "main": "index.js",
  "scripts": {
    "generate": "prisma generate",
    "push": "prisma db push",
    "migrate": "prisma migrate dev",
    "seed": "tsx seed.ts",
    "studio": "prisma studio",
    "reset": "prisma migrate reset"
  },
  "dependencies": {
    "@prisma/client": "^5.6.0"
  },
  "devDependencies": {
    "prisma": "^5.6.0",
    "tsx": "^4.6.0",
    "typescript": "^5.1.3"
  }
}
